# AMQP im Container-Netz; kein Management-Plugin im Image rabbitmq:4.3.
listeners.tcp.default = 5672
# Bewusst nur Passwort-Anmeldung im Basisbeispiel, kein implizites ANONYMOUS.
auth_mechanisms.1 = PLAIN
anonymous_login_user = none
