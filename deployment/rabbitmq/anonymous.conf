listeners.tcp.default = 5672
# RabbitMQ >=4.3: Client sendet keine Credentials, muss ANONYMOUS auswählen.
auth_mechanisms.1 = ANONYMOUS
# Interne Zuordnung; muss zum initialisierten Benutzer aus Compose passen.
anonymous_login_user = mq-demo
anonymous_login_pass = mq-demo-password
